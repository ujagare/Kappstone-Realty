# Kappstone-Realty

Google integrations setup is ready.

Use [.env](c:/Users/ujaga/OneDrive/Desktop/EstateAgency/.env) to store:
- `GOOGLE_ANALYTICS_ID`
- `GOOGLE_TAG_MANAGER_ID`
- `GOOGLE_SEARCH_CONSOLE_VERIFICATION`

Then run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\apply-google-integrations.ps1
```
